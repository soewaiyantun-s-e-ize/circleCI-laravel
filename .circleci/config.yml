version: 2.1

orbs:
  slack: circleci/slack@3.3.0

jobs:
  deploy-and-notify:
    executor: container
    steps:
      - checkout
      - run: ./deploy.sh
      - slack/status:
          success_message: 'abc'
          failure_message: 'nnnnnnnooooooo'
          webhook: '${SLACK_WEBHOOK}'
